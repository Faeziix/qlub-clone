// Vitest stub for the `server-only` package.
// In a real Next.js runtime this module throws when imported from a client
// context.  In Vitest (node environment) there is no such restriction, so
// the stub is an empty no-op — the guard is irrelevant for unit tests.
export {};
